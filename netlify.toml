[build]
  command = "pnpm --filter=@madavi/web run build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"
  NODE_OPTIONS = "--experimental-sqlite"

[functions]
  node_bundler = "esbuild"
