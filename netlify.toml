[build]
  command = "pnpm --filter=@madavi/web run build && cp -r apps/web/.cache apps/web/dist/.cache"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"
  NODE_OPTIONS = "--experimental-sqlite"

[functions]
  node_bundler = "esbuild"
